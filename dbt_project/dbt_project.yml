name: 'pioupiou_wind'
version: '1.0.0'
config-version: 2

profile: 'pioupiou_wind'

model-paths: ["models"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  pioupiou_wind:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
